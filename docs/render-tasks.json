{"format":"pcw-render-tasks","version":1,"updatedAt":"2026-09-10T05:00:00.000Z","tasks":[{"id":"t1-trace-pair","createdAt":"2026-09-08T19:40:00Z","title":"Capture the trace pair on your usual route","criteria":"Two captures, same route: base-game.html?trace=1, then base-game.html?trace=1&tracephases=0. Both saved to the performance log.","why":"Gives the six encode stages as capture-wide percentiles, the instrumentation's own cost, the granted device limits, and names any shader compile.","status":"done","doneAt":"2026-09-08T13:30:57.866Z","notes":"","files":[],"outcome":"Read. Device grants 8 vertex-stage storage buffers, so the pulled forest is admitted. The overhead pair was inconclusive: the three timers-off runs (p50 57, 57, 32 ms) varied more than the timers-on runs differ from them."},{"id":"t2-crafts-grass","createdAt":"2026-09-08T19:40:00Z","title":"Look at drones, vehicles and grass","criteria":"Spawn a drone and a vehicle and fire an AGM: no missing part, wrong colour, or see-through hull. Watch grass wind, walk across a rebase, then repeat with ?grassScope=object: no frozen wind, wrong rate, or grass jump in either run.","why":"These are the two shipped changes whose failure would be visible: shared craft materials and the render-group grass uniforms.","status":"done","doneAt":"2026-09-08T14:00:03.930Z","notes":"","files":[],"outcome":"Ticked with no note, so I am reading it as: nothing looked wrong. If anything did, reopen it with a note."},{"id":"t3-refresh-audit","createdAt":"2026-09-08T19:40:00Z","title":"Run the refresh audit on the route","criteria":"base-game.html?refreshaudit=1 on the same route, then save a capture.","why":"It never skips; it reports where a skip of the refresh would have been wrong on the spawn building, structures and roads, and what its own check costs.","status":"done","doneAt":"2026-09-08T14:11:12.512Z","notes":"","files":[],"outcome":"Read, about 33,000 frames. Zero wrong skips, but nearly every declared object changed every frame because its model-view matrix depends on the moving camera, and the check cost 0.4 to 0.6 ms per object. The skip extension is marked not worth building in this form."},{"id":"t4-forest-compact","createdAt":"2026-09-08T19:40:00Z","title":"Forest: default against 'One merged draw, compact'","criteria":"Tree look panel, LOD2 branch submission. Switch to 'One merged draw, compact' after tasks 1 to 3. Far trees still there and same shape as before. If they vanish or go black, note it and switch back.","why":"The pulled forest has compiled headless only; this is its first device compile and first render. The fallback should already have switched back on a validation error and the capture says why.","status":"done","doneAt":"2026-09-08T14:56:30.965Z","notes":"the 2 logs before the lod2 and 1 off are just lod2 off. the stationary lod2 and 1 off is actually lod 012 off. then i kept those off and turned off terrain. then i turned off grass too. i was able to get 65-75 fps, but movement still dips. therse were on base-game.html?refreshaudit=1","files":[],"outcome":"Your note describes the LOD rung toggles: all rungs off took the standing frame from about 24 to 16 ms. That is what removing the vegetation saves (geometry, shading, shadows and culling as well as the objects), an upper bound on any forest change, not a per-object figure. Every capture has the submission select at variants, so the compact mode itself has not been rendered yet. Reassigned as task 6."},{"id":"t5-push","createdAt":"2026-09-08T19:40:00Z","title":"Decide on pushing the branch","criteria":"Say push, or not. About 36 commits since 492de74 are local; the friend test runs off 492de74.","why":"Task 2 came back clean, so the visible risks left are the forest modes, which are off by default.","status":"done","doneAt":"2026-09-09T03:50:00Z","notes":"[user: push]","files":[],"outcome":"Pushed: 48 commits, 492de74 to e08ebea, on sp1-webgpu-renderer-migration. Not pushed because not committed: your working-tree edits to base-game-default-state.json (the shipped default you saved), docs/subsystems/ordination.md, ordination-steps.html and the grass-investigation WGSL dumps. Say if the shipped default should be committed. A friend pulling now gets the vendored Three, the shared light uniforms, the resumable tree build, the budgeted field delivery and the structures clearance fix; the room server needs a restart to read anything new."},{"id":"t6-forest-compact-render","createdAt":"2026-09-08T21:40:00Z","title":"Render the compact forest mode and compare it to the default","criteria":"Tree look panel, 'LOD2 branch submission', with all three LOD rungs ON and trees on. Stand in one spot facing a tree line. Capture once at 'One draw per variant', then switch to 'One merged draw, compact' (the forest rebuilds), same spot, capture again. Note whether the far branches look identical, and whether anything vanished or went black. If the console shows a forest fallback warning, note its message.","why":"The device admits the mode, but it has only ever compiled headless. This is its first render. Same spot, same content, the two captures are the only fair test of what merging one role saves or costs; do not have ?trace=1 or ?refreshaudit=1 on for these.","status":"done","doneAt":"2026-09-08T19:48:07.133Z","notes":"logs in . no obvious difference between them. but the one merged draws increase the tris from 500k to 50m","files":[],"outcome":"Read: three captures at the same spot (19:42 variants, 19:44 slots, 19:45 compact), no flags. Both merged modes rendered with no fallback and all 16 variants visible; you saw no visual difference. Forest draws went 97 to 82 (the 16 far-branch meshes to 1) and the frame did not improve: p50 18.1 ms at variants against 19.4 and 20.3 merged, p95 23.8 against 42.2 and 31.2, within these runs’ noise. The 50M triangles is the declared ceiling (index count × the geometry’s instance ceiling, three.webgpu.js:81505), not what the GPU drew; GPU time for the pair was not captured. Verdict: merging one role does not pay here. The option stays default-off and is not recommended. Fixed-threshold view: frames over 33 ms per 1000 were 2 at variants, 82 slots, 36 compact; none over 50 in any of the three. Scoped to this role, layout and these three runs; GPU cost of the merged draw stays unmeasured."},{"id":"t7-worker-ab-minimal","createdAt":"2026-09-08T21:40:00Z","title":"Walking A/B: one terrain worker against two, on the minimal scene","criteria":"Trees off (all LOD rungs off) and grass off, as in your 14:50 runs. Walk the same route twice, about 30 s each: base-game.html?terrainworkers=1, then base-game.html (default, 2 workers). One capture each.","why":"With trees and grass off the frame sits at 15 to 17 ms standing but still dips walking, and the dip frames carry busy terrain workers and gaps between frames. One worker against two on the same route tests what the setting does to those dips. It does not say why a frame was late; that needs a DevTools trace.","status":"done","doneAt":"2026-09-08T20:00:27.077Z","notes":"one worker j","files":[],"outcome":"Read: one worker (19:55:13, 19:55:39) against two (19:48:51, 19:49:10, 19:49:57), trees and grass off, walking. Frames over 50 ms per 1000: 15 and 2 at one worker against 77, 7 and 10 at two; over 33 ms: 53 and 74 against 426, 182 and 114. The one-worker runs sit at the low end of a two-worker spread that is wider than the difference, so with two runs against three no effect is established either way. Spike frames carry the same signature in both."},{"id":"t8-gpu-timestamps-walking","createdAt":"2026-09-08T21:40:00Z","title":"GPU timestamps on while walking","criteria":"Same minimal scene and route as task 7, default workers: base-game.html?gputime=1, walk about 30 s, one capture. Note if the page says the device does not support timestamps.","why":"The dip frames show 20 to 30 ms of gap between frames with no long task on the main thread. If GPU time rises in those frames the GPU is involved; if not, it is not. Either way this does not by itself attribute the gap. Timestamp frames are not comparable with untimed ones, so this is its own capture, with no trace or audit flag.","status":"done","doneAt":"2026-09-08T20:10:57.902Z","notes":"","files":[],"outcome":"Read from your 20:03 to 20:08 runs (both with and without trees). GPU render passes: p50 0.33 to 0.46 ms, p95 under 1 ms, worst frame 1.9 ms; compute under 2 ms with trees on. GPU pass execution is not where the 40 to 130 ms frames go. The frame is CPU-bound: the render call takes 11 to 16 ms of main-thread time at 180 to 260 draws with trees and grass off. Caveats: the figure is the last resolved frame’s pass total, a frame or two late, quantized in steps of about 65 µs, and it excludes presentation and queue wait."},{"status":"done","doneAt":"2026-09-08T23:10:00Z","notes":"","files":[],"createdAt":"2026-09-08T20:40:00Z","id":"t9-allocation-sampling","title":"Record where the page allocates while standing still","criteria":"Minimal scene (trees and grass off), stand still, no URL flags. DevTools, Memory tab, \"Allocation sampling\", Start, wait 10 s, Stop. Save the profile (right-click the profile in the left list, Save) into research/stats/ and attach the path here.","why":"Every capture, including the three standing still with nothing moving, shows the JS heap growing 22 to 46 MB per second, with a garbage-collection-sized drop of about 24 MB every second or so. Spike frames carry such a drop three to ten times more often than ordinary frames, though most spikes have none. The profile names the allocating functions; nothing in our capture can. The profiler samples allocations and adds its own overhead while recording, so frame times during the profile are not comparable with the captures; only the allocating functions and their shares are read from it. Your 21:40 moving trace makes this more pointed: five of its biggest dips (90 to 155 ms frames) were major garbage collections of 68 to 114 ms each, with the heap at 162 to 243 MB before and 135 MB after; the stationary trace had none. The collection is measured; what allocates is not, and the per-texel road queries above are a code-supported candidate, not a proven one.","outcome":"Read: seven profiles from 19:00Z (before tasks 11 and 12) and two from 19:12Z on the first build of 11 and 12. Recorded with discarded objects included, so they are allocations over the run. Standing still, no trees (1.0 GB sampled): Three’s uniform update bookkeeping is the largest single allocator at 15 to 17% (a range object and a Map entry per changed uniform per object per frame); then the terrain batch-visibility walk each frame (8%), the kill-plane check evaluating the volumetric density field every frame (7%), the legs’ ground probes collecting every ray hit as objects (14% with map-collision), and the field-derivation road queries (10%). The two profiles on the first 11+12 build found a bug of mine: road queries at 32% and 45% of 2.9 and 4.5 GB, because a landed-but-undelivered tile was neither queued nor in flight, so the window re-requested it every frame and duplicate builds fed the backlog. That is what starved the trees and greyed the grass. Fixed in 48e46c2 (landed tiles stay in the dedupe), e052abd (segment-run index, 5x cheaper queries) and 5803a70 (budget grows with backlog). No profile of the fixed build yet."},{"status":"done","doneAt":"2026-09-08T21:20:00Z","notes":"","files":[],"createdAt":"2026-09-08T20:40:00Z","id":"t10-devtools-trace-walking","title":"DevTools performance recording while walking: not needed yet","criteria":"Nothing to do now. Astra re-read the 2026-09-07 recording task by task and it already names concrete work (see task 11 and the Logs tab). A new recording is only worth your time after a change lands, to check it, or for a measurement this one lacks; I will re-open this with a specific ask if so.","why":"Aligned by their raw start and end, the long tasks are the page’s own frame callback. In the existing recording the slow ones hold: render preparation that roughly doubles, a synchronous tree chunk build of 20 to 22 ms in two of them (trees on only), GC in 9 of 137, 30 to 52 ms of the thread not running at all in the longest, and three late tasks that were browser layout work with no frame. Several contributors, none explaining all of it.","outcome":"Closed without a recording: the existing 2026-09-07 trace, re-read task by task, already gave the concrete targets. Report in research/stats/trace-task-review-20260907.md."},{"id":"t11-tree-build-decision","createdAt":"2026-09-08T21:20:00Z","status":"done","doneAt":"2026-09-08T22:30:00Z","notes":"[user: yes to 11 and 12, go ahead]","files":[],"title":"Decide: make the tree chunk build resumable across frames","criteria":"Say yes, no, or ask. Yes means I change base-game-trees.js so one chunk’s placement and ground-height work spreads over several frames under the existing budget; same seed, same candidates, same heights, so the trees land in the same places, only the frame a chunk appears in changes. Node test of placement parity before and after; you look at a tree line while walking for pop-in.","why":"Today buildChunk places about 41 candidates (with shore samples) and then asks the volumetric ground height for each, all inside one frame; the per-frame budget is only checked between chunks, so it cannot stop a slow one. Astra measured that build at 20.8 and 22.4 ms inside two 76 to 79 ms frames in the 2026-09-07 recording (with the ground scans 11 and 8 ms and the surface height 5.5 and 5.7 ms of that). It is the one concrete extra-work path named so far; it applies only with trees on and does not explain the plants-off dips. The forest slot in our captures times the GPU pass only, so this build sat in the “rest” column of the spike table. The alternative, moving the height loop to a terrain worker, is larger and changes a contract; I would start with the resumable build.","outcome":"Yes, shipped. base-game-trees.js builds a chunk as a job: placement (forest-placement createPlacementJob, pausing every 16 attempts) then ground heights one record at a time, each drain doing what fits before its deadline; flora-chunks resumes a paused chunk first and abandons it if the window moves. Same seed, candidates and heights: the parity test runs the whole forest at a zero budget and gets byte-identical records and ground heights. Look for pop-in along a tree line while walking."},{"id":"t12-field-derive-decision","createdAt":"2026-09-08T21:50:00Z","status":"done","doneAt":"2026-09-08T22:30:00Z","notes":"[user: yes to 11 and 12, go ahead]","files":[],"title":"Decide: take the field derivation off the worker reply and out of one frame","criteria":"Say yes, no, or ask. Yes means: worker replies are queued and processed under a per-frame budget instead of inside the message handler; a tile’s cover derivation can pause between texel rows and resume next frame, publishing the tile only when complete; the road distance query gets a distance-only path that allocates nothing per segment. Same inputs, same outputs (Node parity tests on the derivation and the distance query). You walk the same no-plants route and look for late or missing ground cover.","why":"Your 21:40 moving trace, plants off: one 80 ms main-thread task was the handler for a terrain worker reply. It derived ground cover for every texel of the arriving tile, and for each texel asked the road network how far the nearest trail is, which allocates a Set and a result record per segment; that polyline projection alone held 50 ms of it. Three more reply handlers nearby took 22, 46 and 22 ms. This runs with vegetation off because the field feeds other systems too, so it is not to be simply skipped. Confirmed in current code: the scheduler delivers synchronously from onmessage, and derive has no budget inside a tile.","outcome":"Yes, shipped. Worker replies now land in a queue and pump() hands them to their windows under a 2 ms budget; flora-field derive pauses between texel rows and attaches the channels only when done; the road index’s bounded nearestDistance walks its buckets with reused Sets and the polyline distance no longer builds a record per segment. Parity tests: derive paused per row equals one-shot; the scalar segment distance is bit-identical; nearestDistance agrees with a full walk inside the radius. Walk the no-plants route again and look for late or missing ground cover."},{"id":"t13-verify-resumable","createdAt":"2026-09-08T22:30:00Z","status":"done","doneAt":"2026-09-09T02:13:00Z","notes":"[user: plant captures in; nothing looks weird]","files":[],"title":"Walk the no-plants route and the tree line after the two changes","criteria":"Reload first: the build must include 48e46c2 (the Tasks tab is served from the same tree, so a hard reload of the game page is enough). Then the same no-plants walking route as your 21:40 recording, no URL flags, one capture; then trees on, walk toward a tree line, one capture. Note grass height, tile colour, and whether trees appear as you walk. A DevTools recording of the no-plants walk, saved into research/stats/, is the direct check that the 80 ms reply handler is gone. One more allocation-sampling profile while walking would show the fixed build’s allocators.","why":"Your first look at the 11+12 build showed grass on stale field heights, then grey grass and no trees: the field windows were starving on duplicate rebuilds (task 9 outcome). Three commits since fix that; the walk is the check. The stutter you saw go away is the reply handler off the message, and should stay gone.","outcome":"Read (02:10 to 02:13Z, vendored build, structures off). Trees and grass on: standing 13.4 ms p50, the same as without plants; walking 25.3 ms p50, p95 43.6, 16 frames per 1000 over 50 ms and 205 over 33. Trees and grass off, same session: walking 13.4 to 16.4 p50, none over 50, 4 to 45 over 33. So on this build plants cost nothing standing and about 9 to 12 ms walking, and in this session the frames over 50 ms appeared only with plants on; earlier sessions on this build had over-50 frames with plants off (collections, the reply handler), so this is what this session showed, not a general rule. You saw nothing wrong. What inside the plants costs it is not in these captures (no trace flag); task 19 asks for that walk with the trace on."},{"id":"t14-structures-ab","createdAt":"2026-09-08T23:40:00Z","status":"done","doneAt":"2026-09-09T00:05:00Z","notes":"[user: structures on/off heaps in; structures definitely on in these two]","files":[],"title":"Structures on against off, walking the same route","criteria":"On the fixed build, plants off. Walk your route twice for about 30 s: once with Structures on (default), once with the Structures toggle off. One capture each. Note whether the occasional stutters go with the toggle.","why":"You suspect the remaining stutters are the structures. The code supports it as a candidate: base-game-structure-collision.js builds one structure tile per frame as one unit (the model, its scatter and a BVH bake) with no time budget inside it, the same shape as the tree chunk build was. Its build time is recorded per tile but not in the capture series, so the toggle is the quickest test; if it is the cause, the fix is the same resumable shape as task 11.","outcome":"Read, and you were right. Structures on: 1.33 GB of a 3.85 GB allocation run (35%) is the structures clearance asked once per texel of every arriving field tile, and for each texel it walked every resident building’s floor slabs anywhere in the world, allocating an options object and boxed doubles per call. Structures off: that entry is absent. Fixed: each dressed tile keeps a box (slabs expanded by margin plus fade) and is skipped when the point is outside it, which is exact because every slab answers 1 out there (tested); the per-call options object is gone. The one-tile-per-frame structure build itself is not in these profiles as an allocator; whether it is the remaining stutter needs a capture or recording on the fixed build."},{"id":"t15-uniform-names","createdAt":"2026-09-09T00:30:00Z","status":"done","doneAt":"2026-09-09T00:15:00Z","notes":"[Fable: the 20:00 heap profiles and DevTools traces on bf189b5 were taken with ?trace=1; in them the trace’s own bookkeeping is the top allocator, so they do not compare with the earlier profiles and do not name the uniform. Not wasted: the two DevTools recordings go to Astra for long-task reading on the fixed build.]","files":[],"title":"One standing capture with the trace on, saved to the performance log (not a heap profile or DevTools recording)","criteria":"base-game.html?trace=1, plants off, stand still. Use the game’s own performance capture, the panel control that says “Measuring performance…” and writes to research/stats/base-game-performance-log.json. The uniform names are in that file only; a heap profile or a DevTools recording taken with ?trace=1 does not carry them.","why":"The allocation profiles put Three’s per-changed-uniform bookkeeping first among allocators standing still. It fires only for a uniform whose value changed on that object that frame. Three’s own per-object uniforms are matrices, so a Vector3 changing on every object every frame is most likely one of ours in the per-object group of a shared material: the same value written once per object instead of once per frame. The trace now records the changed uniforms by group and name; this capture names them, and the fix is the render-group move the grass wind already had.","outcome":"Read (00:14Z, standing, plants and structures off, 92 objects). Standing still the per-object refresh writes very little: 37 binding writes a frame against 106 refresh checks, and the shadow passes write nothing. The rows name four per-object uniforms rewritten every frame, but only by Three’s generated names (nodeUniform61, 8, 6, 29), which I cannot map to a source. The trace now adds the value type, the node’s own name and the object and material being encoded, so one more standing capture names them. And since standing writes are this few, the allocation the profiles charged to this path must come from movement, when every object’s model-view matrix is rewritten; a walking capture with the trace on measures that directly. Task 16."},{"id":"t16-trace-pair-fixed","createdAt":"2026-09-09T01:05:00Z","status":"done","doneAt":"2026-09-09T00:27:00Z","notes":"","files":[],"title":"Two captures with the trace on: standing, then walking","criteria":"Reload (the build must include the new trace rows). base-game.html?trace=1, plants and structures off. One capture standing still, then one capture walking your route. Game performance capture both times.","why":"The standing capture showed only 37 uniform writes a frame, four of them per-object uniforms named only nodeUniformN. The rows now carry the value type and the object and material being encoded, which names them. The walking capture shows how many writes a frame movement adds, which is what the allocation profiles charged to this path.","outcome":"Read, seven captures 00:24 to 00:26Z, plants and structures off, 92 to 132 objects. Standing: the bindings stage of the main scene is 2 ms with 36 uniform writes a frame. Moving: 9 to 18 ms with 117 to 197 writes, the same objects. The extra writes are the camera view matrix and the light positions, written once per body-part, weapon and debris batch (18 times for the cloth material alone) instead of once per frame. Mechanism, read in Three’s source: those live in the shared render group, but Three appends the object uuid to the material cache key for every instanced mesh (a TODO citing PR 29066), so each instanced mesh gets its own node build, its own light uniform nodes, and therefore its own render bind group; the sharing that would write them once never engages. The four standing per-object writes are water and cloud numbers plus one terrain batch number, small and ours. Decision on the instanced-mesh sharing is task 17."},{"id":"t17-render-group-sharing","createdAt":"2026-09-09T01:30:00Z","status":"done","doneAt":"2026-09-09T02:30:00Z","notes":"[user: try a]","files":[],"title":"Decide how to stop rewriting camera and light uniforms per instanced mesh","criteria":"Say A, B, C, or ask. A: patch our vendored three.webgpu.js so instanced meshes with the same material share one node build and one render bind group (a local change to Three, carried across upgrades; Node-tested against the trace counters, then a walking capture). B: move the body, weapon and debris batches off InstancedMesh onto storage-buffer-driven meshes, the way the forest and grass already draw, so Three shares their build without a patch (larger: body-part-batches and weapon-part-batches rewrite, raycast picking by instance id to redo). C: leave it, and accept 7 to 16 ms of bindings work while moving at this scene.","why":"Measured: while moving, the bindings stage grows from 2 to 9 to 18 ms with the same 92 objects, and the growth is 80 to 160 redundant writes of camera and light uniforms, one per instanced batch. This is the largest single movement cost found so far, and it is the camera-data separation Astra raised, now with a named mechanism in Three’s source. Both A and B keep every image identical; they change only how many times the same bytes are written. I would start with A behind a flag, because it is small and reversible, and because B is a rewrite of two working modules.","outcome":"A shipped. Base Game now serves its own copy of Three 0.184 from vendor/three-0.184 with one patch: the light and shadow uniform nodes that each material build used to create afresh are cached per light, so instanced meshes of one material share one render bind group. Node test: two instanced builds of one material get identical render-group uniform nodes with the patch on and different ones with it off. ?sharedlights=0 turns the patch off for an A/B. Unseen in a browser; task 18 is the check."},{"id":"t18-shared-lights-check","createdAt":"2026-09-09T02:30:00Z","status":"done","doneAt":"2026-09-09T01:54:00Z","notes":"[user: nothing looks weird in default]","files":[],"title":"Check the shared light uniforms: look, then the trace pair, then the A/B","criteria":"Reload (the page now loads Three from vendor/three-0.184). First look: lighting, the flashlight and its shadow, the sun shadow, bodies and weapons must look as before; note anything dark, flickering or unlit. Then base-game.html?trace=1, plants and structures off: one capture standing, one walking. Then the same two with base-game.html?trace=1&sharedlights=0. Four captures.","why":"The patch changes which uniform nodes a light hands each material build; the images should be identical, but that is a claim until you see them. The walking capture is the measurement: uniform writes a frame should fall from 117 to 197 toward the 37 of standing still, and the bindings stage from 9 to 18 ms toward 2 to 3. The sharedlights=0 pair is the control on the same build.","outcome":"Read (01:49 to 01:53Z, plants and structures off, same route as last night). Walking with the patch: 66 to 68 uniform writes a frame (117 to 197 last night), main-scene bindings stage 2.0 to 3.3 ms (9 to 18), encode 6 to 8 ms (18 to 21), frame p50 17.2 to 17.5 ms (21.8 to 40.6), p95 26 to 36 (42 to 72), no frame over 50 ms in either walk (5 to 243 per 1000 last night). Standing unchanged at 33 writes and 13.3 ms. The remaining moving writes are legitimate per-object matrices (18 unnamed basic-material meshes, the points, the laser beam, water, clouds). Your look found nothing wrong. The two control captures did not run as a control: the URL was ?trace=1?sharedlights=0, so neither flag parsed; they are plain captures with the patch on (p50 17.4 walking, matching the traced ones, so the trace itself costs little here). The before numbers are last night’s captures on the same route, a different session, so the comparison carries session variation. If you want the clean control, one walking capture with base-game.html?trace=1&sharedlights=0 (ampersand) gives it; not required. 02:05Z: the control still did not run; the URL was ?trace=1&?sharedlights=0 and the stray ? made the key ?sharedlights. No capture in the log has the flag parsed yet."},{"id":"t19-plants-walk-traced","createdAt":"2026-09-09T02:20:00Z","status":"done","doneAt":"2026-09-09T03:09:00Z","notes":"[Fable 02:51Z: two captures landed with the control flag parsed, both STANDING with plants off: sharing off standing costs bindings 2.6 to 2.8 ms p50 against 0.9 to 1.1 shared and encode 7.3 to 8.0 against 3.7, at the same 36 writes, because 18 separate render groups are diffed even when nothing changed. Still missing: the WALKING control, and the walking capture with trees and grass on.]","files":[],"title":"Two walking captures with the trace on: plants on, and the sharing control","criteria":"Same route. 1) base-game.html?trace=1 with trees and grass on, structures off: one walking capture. 2) base-game.html?trace=1&sharedlights=0 with plants off: one walking capture. Type the second address exactly; there is one question mark, after html, and one ampersand. The Session card shows the URL the page saw.","why":"Capture 1 splits the 9 to 12 ms that trees and grass add while walking into encode stages, uniform writes and the plant slots, and shows what the frames over 50 ms hold now that the tree build is resumable. Capture 2 is the missing control for the shared light uniforms: the same build and session with the sharing off.","outcome":"Read. The control landed (02:54Z, walking, plants off, sharing off, same session and route as the 01:50Z shared runs): frame p50 42.5 ms against 17.2 to 17.5, p95 69.5 against 26 to 36, 365 frames per 1000 over 50 ms against 0; uniform writes 121 a frame against 66 to 68; bindings stage 17.4 ms p50 against 2.3; encode 26.4 against 7.4 to 7.7. Astra checked the series: the runs are not matched in movement. The sharing-on walks held about 6.2 m/s for most of their frames; the control averaged 2.75 m/s and never reached 5 m/s, and it was an hour later. So 42.5 to 17 ms is an observed cross-run comparison on the same build and route, not an isolated measurement of the patch; the same-frame counts (writes 121 against 66 to 68, bindings stage 17.4 against 2.3 ms) are the isolated part. The plants-on traced walk (03:08 and 03:09Z, trees and grass on, sharing on): frame p50 33 ms, p95 51 to 64, 63 to 173 per 1000 over 50 ms; 186 to 207 main-scene objects against 94; encode 17 to 18 ms p50 against 7.5; bindings 4.7 against 2.3; nodes 5.1 to 5.4 against 1.5; draw 1.6 to 1.9 against 1.2; the forest and grass slots themselves under 1 ms. So the plant cost while walking is about 100 more objects through the encode, roughly 0.1 ms each, not the plant systems’ own CPU. Frames over 50 ms carry 5 to 12 ms more render call, twice the gap before the frame, long tasks of 49 to 56 ms and more terrain in flight."},{"id":"t20-forest-object-count","createdAt":"2026-09-09T03:20:00Z","status":"done","doneAt":"2026-09-09T03:40:00Z","notes":"","files":[],"title":"Optional: forest look settings that also cut object count (your preference, not a prerequisite)","criteria":"Nothing required. If you ever want a lighter forest, the Tree look panel has variants per species, the LOD rungs and leaf shadows, and each object dropped is worth about 0.1 ms of walking frame by the traced ratio. The implementation route that keeps the picture is task 21.","why":"Astra’s point, agreed: acceptable frame rate through reduced quality must not be the default answer. The forest’s cost while walking is per-object encode work, and task 21 removes most of that work without removing objects.","outcome":"Reframed as optional after review; the implementation route is task 21."},{"id":"t21-static-refresh-skip","createdAt":"2026-09-09T03:40:00Z","status":"open","doneAt":null,"notes":"","files":[],"title":"Forest refresh policy: being built as an off-by-default experiment (no decision needed from you)","criteria":"Nothing yet. Astra reviewed the design and said to proceed with the implementation off by default under the existing go-ahead, once four invariants are in code and tests: material value changes that keep the shader must still refresh every mesh; the normal-matrix uniform’s camera dependence must be shown from the source and any graph with an unknown per-object uniform must be refused; the bookkeeping must record every refresh; and adversarial tests (mutate one mesh’s geometry, alternate which mesh comes first, rebuild, rebase, main plus shadow plus reflection in one frame, flag off) must exist. An agent is building it now. When it lands you get one ask: your walk with ?trace=1&foreststatic=1, and the trace’s refreshed-over-checked count is the measurement. Turning it on by default waits for that.","why":"The contract Astra asked for is written (scratchpads/fps-churn/static-skip/, three reports from the generated shaders and the served Three’s source). What a forest mesh privately owns: its slot offset, its world and normal matrices, the scale uniforms and a handful of material scalars, its own geometry and indirect buffer. What can change them, and who catches it: material and texture changes and the double-sided toggles bump the material version and Three rebuilds the render object; the render-origin rebase and every cull uniform ride the compute path, which is not gated; the rung gate, mirror pass and fallback only touch visibility; a palette rebake makes new meshes. What Three would NOT catch and forest-gpu must mark itself: the tree-scale, leaf-scale and leaf-sway writes (guarded on value change), a variant install swapping a mesh’s geometry and flagging its indirect buffer, and arena uploads in the pulled modes. One standing rule follows: a forest material property is only ever written together with needsUpdate, true today, enforced by the test. Why no Three patch: the refresh policy is an ordinary overridable material method whose only consumer is the refresh check, so the change stays inside forest-gpu.js and every other material keeps Three’s behaviour. Not promised: any millisecond figure. The recordings put render submission at 53 per cent of trees-on wall time, bindings 10 and node updates 8; this removes most of the forest’s share of those two, and the trace count says how much."},{"id":"t22-leaves","createdAt":"2026-09-09T04:20:00Z","status":"done","doneAt":"2026-09-10T02:50:00Z","notes":"[user: several traces, heaps and logs in; the last ones are trees on; some logs labelled trees off are trees on]","files":[],"title":"Leaves: the first fix is in; reload, look at the canopy, then one plants-on walk with the trace","criteria":"When you next have a minute, only two walks are still wanted: base-game.html?trace=1 with trees on (default), then base-game.html?trace=1&leafnormal=fragment. Same route. The trace stages are the trustworthy measurement here; the GPU columns are not, for now.","why":"Astra: a leaves-off capture is a different workload, not a before/after of the fragment fix, so the fix now has its own same-content switch (?leafnormal=fragment). Captures a and b measure the fix; c and d separate the leaf and leaf-shadow GPU cost as diagnostics; e gives the CPU side after the fix.","outcome":"Read the eight ?gputime=1 captures (02:10 to 02:20Z) with the forest state taken from the capture itself, not the label. None carries the leafnormal=fragment form and none has the leaves or leaf-shadow toggle off, so the fragment fix and the shadow role are still unmeasured in frame time. What they do show, same session: trees off walking 18.5 ms p50 with a 10.3 ms render call; trees on walking 24.6 to 44.7 ms p50 with a 16.5 to 25.3 ms render call, worsening through the session as the resident trees grew from 1,140 to 1,636. Standing with trees 14.6 against 13.3. The lag you feel with trees is main-thread submission, not the GPU. The GPU timestamps themselves I no longer trust as whole-frame numbers: they read 0.2 to 0.4 ms while moving and 1.6 ms standing, and a moving frame cannot cost the GPU less than a standing one; the resolve path can hand back a stale value while a resolve is in flight, so those columns are set aside until fixed. The two trees-on heap profiles show the Three uniform-range allocation gone since the light sharing; what remains is ours (road queries per texel, the player capsule against the terrain BVH, the kill-plane density scan every frame, per-texel cover objects, the batch-visibility walk), and an agent is on a same-content allocation pass for those now. The three DevTools recordings (22:12, 22:16, 22:20) go to Astra for long-task reading with trees on. Closed 02:50Z: the fragment fix stands on the generated-shader test; the leafnormal=fragment pair never ran and is no longer asked for. The trace walks put the plant cost where the census did: about 110 more objects through the encode, 7 to 12 ms, which is the refresh skip in task 21. 04:10Z, the DevTools recordings read: trees off 16.6 ms p50 with 1 interval over 50 ms in 800; trees on 30 to 36 ms p50 with 11 to 19 per cent of intervals over 50 ms; render samples are 53 per cent of the trees-on wall time, bindings 10 and node updates 8; GC 3 to 4 per cent but in the worst frames; the capsule collision 4; the tree build never more than 2.2 ms in a worst task. With ?trace=1 on, the trace itself is the largest app-level sample owner, so traced frame times are inflated."},{"id":"t23-mid-leaf-lod","createdAt":"2026-09-09T20:30:00Z","status":"open","doneAt":null,"notes":"","files":[],"title":"Optional look decision: give the middle LOD rung its own, sparser leaves","criteria":"Nothing required. If you want it: tree-viewer.html has midLeafRatio and midLeafSizeMult (defaults 1 and 1, which is why the middle rung draws the full near-leaf geometry). Try, say, ratio 0.5 with size 1.4 and look at trees at 60 to 140 m; if the canopy reads the same, tell me the numbers and I wire them into the Base Game bake (base-game-forest.js never passes them).","why":"The census (scratchpads/fps-churn/leaves/03-leaf-census.md): the middle rung’s leaves are the largest single leaf role, 153,000 to 163,000 triangles, 45% of that rung, because leavesMid is only built when the two settings are passed and Base Game never passes them. Fewer, larger cards there is what the rung was designed for, but it changes the picture at that distance, so it is your call, not a default I will change."},{"id":"t24-leaf-normal-space","createdAt":"2026-09-09T21:00:00Z","status":"open","doneAt":null,"notes":"[Fable 03:45Z, from the generated WGSL (node test-forest-leaf-shaders.mjs --dump, leavesL0 fragment lines 233 to 239): normalView = the yaw-rotated normalLocal with no camera transform, then dot(normalView, light direction in view space). Headless evidence done; the quarter-turn demonstration is next when you are at the game.]","files":[],"title":"Forest lighting defect: the normal is in the wrong space (a correctness fix, demonstrated before it is applied)","criteria":"Headless part done (see the note). Your part: stand still under a low sun near a tree, turn the camera a quarter turn, and say whether the lit side of the canopy moves with you. Then I put the fix in behind a flag that keeps today’s rendering and you compare.","why":"Astra read the served Three: NodeMaterial.setupNormal returns the custom normalNode directly and normalView consumes it as view space, with no transform and no face flip; the forest supplies a world-space normal. That is a lighting bug, not a quality preference, and Astra asked that it not be framed as an optional downgrade. It predates the fragment fix. Cost of the fix: one matrix multiply per vertex."},{"id":"t25-allocation-pass-check","createdAt":"2026-09-10T01:00:00Z","status":"done","doneAt":"2026-09-10T02:50:00Z","notes":"[user: new data in]","files":[],"title":"After the allocation pass: one walking heap profile, trees on","criteria":"Reload. Walk your route with trees and grass on for about 30 s with DevTools allocation sampling running (as before, discarded objects included), save it into research/stats/. Nothing else; the two ?trace=1 walks from task 22 still stand if you get to them.","why":"Four same-content changes landed in the code that allocated most while walking: the road distance query no longer boxes a number per segment, the kill-plane check is cached to half a metre of travel instead of scanning the density field every frame, the terrain batch walk reuses its lists, and the field derive writes into scratch objects. Each has a parity test (bit-identical distances over 4,400 queries, identical cover channels over 37,056 values, cache equals the exact plane at every recompute). Whether the 68 to 114 ms collections shrink or only move is not measurable headless; the profile says.","outcome":"Read the two new heap profiles (22:47 trees on, 22:50 plants off). Both were taken with ?trace=1 on, so the trace’s own bookkeeping is a third of each and they do not compare with the earlier ones as totals; but by function the pass shows: the road query fell from 167 to 208 MB to 42 MB, the kill-plane density scan and the per-texel cover objects left the top list entirely, and the batch walk stayed at 57 MB because its Map iteration still built a key-value pair per chunk per frame (fixed now, 6e0 commit below). What remains of ours is the player capsule against the terrain, inside the collision library. The trace walks: plants on 25.8 to 35.1 ms p50 with 204 to 207 objects and 14 to 19 ms of encode; plants off 16.1 with 94 objects and 6.6 ms. Still no capture with leafnormal=fragment; I will stop asking for it and take the fragment fix on its shader evidence."}]}